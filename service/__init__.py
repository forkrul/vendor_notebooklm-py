"""FastAPI service wrapper around the notebooklm-py client.

This package is a thin REST/Swagger layer over the library. It is intentionally
kept separate from `src/notebooklm/` so upstream merges from origin remain clean.
"""
